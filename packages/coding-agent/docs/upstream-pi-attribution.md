# Upstream Pi Attribution

Millrace CLI is derived from Pi by Earendil Works.

The source import recorded for this repository is:

```text
https://github.com/earendil-works/pi at commit fc8a1559
```

The upstream Pi source is MIT licensed. The upstream MIT license text is preserved at `licenses/PI-MIT-LICENSE`. Millrace-specific additions are covered by the repository Apache-2.0 license.

Relevant repository files:

- `UPSTREAM_PI_SOURCE.md`
- `LICENSE`
- `licenses/PI-MIT-LICENSE`

Inherited package names such as `@earendil-works/pi-agent-core`, `@earendil-works/pi-ai`, and `@earendil-works/pi-tui` remain in the dependency graph because Millrace CLI intentionally preserves Pi's core runtime and TUI infrastructure.
